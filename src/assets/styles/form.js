import { StyleSheet } from "react-native";

export const formStyles = StyleSheet.create({
    formGroup: {
        marginBottom: 15
    },
    label: {
        fontWeight: "bold",
        marginBottom: 5,
    },
    input: {
        borderWidth: 1,
        borderRadius: 5,
        borderColor: "#ccc",
        padding: 10,
    },
    error: {
        color: '#f00',
    },
})